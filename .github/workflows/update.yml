name: Update Vulnerability Radar Data

on:
  workflow_dispatch:
  schedule:
    - cron: "5 * * * *" # every hour, offset by 5 min to avoid NVD ZIP generation at :00

permissions:
  contents: write

jobs:
  update:
    # Only run on forks, not the upstream repo (fork & go design)
    if: github.repository != 'RogoLabs/VulnRadar'
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Get date for cache key
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Cache NVD Data
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: .nvd_cache
          key: nvd-${{ runner.os }}-${{ steps.date.outputs.date }}
          restore-keys: |
            nvd-${{ runner.os }}-

      - name: Run ETL
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python etl.py --nvd-cache .nvd_cache

      - name: Update README metrics
        run: |
          python scripts/update_readme_metrics.py

      - name: Commit updated data
        run: |
          set -e
          if [ -z "$(git status --porcelain data/radar_data.json data/radar_report.md README.md)" ]; then
            echo "No changes to radar outputs"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/radar_data.json data/radar_report.md README.md
          git commit -m "chore: update radar outputs [skip ci]"
          git push
